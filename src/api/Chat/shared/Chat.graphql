type Chat {
  id: Int!
  messages: [Message]! # message 모음
  passengerId: Int!
  passenger: User!
  driverId: Int!
  driver: User!
  ride: Ride!
  rideId: Int!
  createdAt: String!
  updatedAt: String
}

# Chat와 Message는 서로 relationship 이다.
# Chat(messages) <---> Message(message)

# Chat와 Ride는 서로 relationship 이다.
# Chat <---> Ride
